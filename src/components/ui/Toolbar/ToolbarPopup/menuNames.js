import { SLICER_MENU_NAME } from './SlicerMenu'
import { TEST_CHARGE_MENU_NAME } from './TestChargeMenu'
import { EFIELD_MENU_NAME } from './EFieldMenu'
import { GAUSSIAN_MENU_NAME } from './GaussianMenu'

// Map popup/window IDs to their display names
export const POPUP_DISPLAY_NAMES = {
  Slice: SLICER_MENU_NAME,
  TestCharge: TEST_CHARGE_MENU_NAME,
  EField: EFIELD_MENU_NAME,
  Gaussian: GAUSSIAN_MENU_NAME,
}
